import React from 'react';
import Input from '../../Forms/Input';

const LoginForm = () => {
  return (
    <section>
      <form>
        <Input />
      </form>
    </section>
  );
};

export default LoginForm;
